# captcha_alpha 核心依赖（识别功能）
ddddocr>=1.5.0
opencv-python>=4.8.0
numpy>=1.24.0
pillow>=10.0.0
onnxruntime>=1.16.0

# MCP Server（可选，AI Agent 接入时需要）
mcp>=2.0.0

# 模型训练（captcha_app 集成训练页 / captcha_trainer）
torch>=2.0
pyyaml>=6.0
loguru>=0.7.0
tqdm>=4.66.0
fire>=0.5.0
